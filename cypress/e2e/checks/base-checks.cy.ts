describe('Base Checks', () => {
    it('Site should contain sitemap.xml',() => {
        // ping sitemap.xml
        // should respond with 200
        // should have at least one entry for the sitemap
        // contents should be valid xml
    })
})
